<p>Upload your photo here:</p>

<%= form_for @photo, html: {multipart: true}, class: "form-horizontal" do |form| %>

  <fieldset class="form-group">
    <%= form.label :title, "Title: ", class: "col-sm-2 control-label" %>
    <div class="col-sm-10"><%= form.text_area :title, class: "form-control" %></div>

  </fieldset>

  <fieldset class="form-group">
    <%= form.label :image, "Image: ", class: "col-sm-2 control-label" %>
    <div class="col-sm-10"><%= form.file_field :image %></div>
  </fieldset>

  <fieldset class="form-group">
    <%= form.label :comment, "Comment: ", class: "col-sm-2 control-label" %>
    <div class="col-sm-10"><%= form.text_area :comment, class: "form-control" %></div>
  </fieldset>

  <div class="actions">
  <%= form.submit "Upload Photo", :class => 'btn btn-success' %>
  </div>
<% end %>
